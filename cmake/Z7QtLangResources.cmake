include_guard(GLOBAL)
# Lang files are embedded in the Qt resource (qrc) and read at runtime via
# ":/z7/lang/..." paths. The POST_BUILD filesystem copy and install rule have
# been removed; this file is kept as an empty stub so existing include() calls
# remain valid.
